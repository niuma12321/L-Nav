{
  "name": "y-nav-backend",
  "version": "0.1.6",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production",
    "d1:migrate": "wrangler d1 execute ynav-db --file=./database/schema.sql --remote"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "wrangler": "^4.0.0"
  }
}
